<Window x:Class="NetworkChangePlaybackAddin.Views.PlaybackWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" Title="ArcGIS Playback" Width="1080" Height="700" MinWidth="840" MinHeight="560" WindowStyle="None" Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource TextBrush}">
  <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" UseAeroCaptionButtons="False" /></shell:WindowChrome.WindowChrome>
  <Window.Resources>
    <SolidColorBrush x:Key="AccentBrush" Color="#0085CA"/><SolidColorBrush x:Key="AppBackgroundBrush" Color="#F5F7F9"/><SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/><SolidColorBrush x:Key="SurfaceAltBrush" Color="#F8FAFB"/><SolidColorBrush x:Key="BorderBrush" Color="#D7DEE5"/><SolidColorBrush x:Key="TextBrush" Color="#17212B"/><SolidColorBrush x:Key="MutedTextBrush" Color="#61707C"/><SolidColorBrush x:Key="HeaderTextBrush" Color="#FFFFFF"/><SolidColorBrush x:Key="HoverBrush" Color="#EDF4F8"/>
    <Style TargetType="Button"><Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="12,7"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Cursor" Value="Hand"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border x:Name="root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8"><ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="root" Property="Background" Value="{DynamicResource HoverBrush}"/></Trigger><Trigger Property="IsPressed" Value="True"><Setter TargetName="root" Property="Opacity" Value=".78"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Setter Property="Background" Value="{DynamicResource AccentBrush}"/><Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/><Setter Property="Foreground" Value="White"/></Style>
    <Style TargetType="TextBox"><Setter Property="Padding" Value="9,6"/><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/></Style>
    <Style TargetType="TabItem"><Setter Property="Padding" Value="18,11"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
  </Window.Resources>
  <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="12"><Grid><Grid.RowDefinitions><RowDefinition Height="44"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Border Background="{DynamicResource AccentBrush}" CornerRadius="11,11,0,0"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="40"/><ColumnDefinition Width="40"/></Grid.ColumnDefinitions><TextBlock Text="ArcGIS Playback" Foreground="{DynamicResource HeaderTextBrush}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0"/><Button Grid.Column="1" Background="Transparent" BorderBrush="Transparent" Foreground="White" Content="−" Click="Minimize_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True"/><Button Grid.Column="2" Background="Transparent" BorderBrush="Transparent" Foreground="White" Content="×" FontSize="18" Click="Close_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True"/></Grid></Border>
    <TabControl Grid.Row="1" Margin="18"><TabItem Header="Record"><Grid Margin="8"><Grid.ColumnDefinitions><ColumnDefinition Width="300"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Border Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16"><StackPanel><TextBlock Text="CHANGE PACKAGE" Foreground="{DynamicResource AccentBrush}" FontSize="10" FontWeight="Bold"/><TextBlock Text="Record a pre-production work package" FontSize="17" FontWeight="SemiBold" Margin="0,5,0,16"/><TextBlock Text="Package name *"/><TextBox x:Name="PackageNameBox" Margin="0,4,0,11"/><TextBlock Text="Source version"/><TextBox x:Name="SourceVersionBox" Text="SDE.DEFAULT" Margin="0,4,0,11"/><TextBlock Text="Work order / reference"/><TextBox x:Name="WorkOrderBox" Margin="0,4,0,11"/><TextBlock Text="Description"/><TextBox x:Name="DescriptionBox" AcceptsReturn="True" TextWrapping="Wrap" Height="72" Margin="0,4,0,16"/><Button x:Name="StartButton" Content="Start recording" Style="{StaticResource PrimaryButton}" Click="Start_Click"/><Button x:Name="SaveButton" Content="Save recording" Margin="0,8,0,0" Click="Save_Click" IsEnabled="False"/></StackPanel></Border><Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><StackPanel><TextBlock Text="RECORDED OPERATIONS" Foreground="{DynamicResource AccentBrush}" FontSize="10" FontWeight="Bold"/><TextBlock x:Name="RecordingSummaryText" Text="Start a recording to capture active-map edits." Margin="0,4,0,12" Foreground="{DynamicResource MutedTextBrush}"/></StackPanel><DataGrid x:Name="RecordingGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="#" Binding="{Binding Sequence}" Width="45"/><DataGridTextColumn Header="Operation" Binding="{Binding Type}" Width="120"/><DataGridTextColumn Header="Layer" Binding="{Binding LayerName}" Width="*"/><DataGridTextColumn Header="Facility ID" Binding="{Binding FacilityId}" Width="140"/></DataGrid.Columns></DataGrid></Grid></Border></Grid></TabItem>
      <TabItem Header="Playback"><Grid Margin="8"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><Border Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="PLAYBACK PACKAGE" Foreground="{DynamicResource AccentBrush}" FontSize="10" FontWeight="Bold"/><TextBlock x:Name="PlaybackSummaryText" Text="Open a package, then play it into the active production map." FontSize="16" FontWeight="SemiBold" Margin="0,5,0,0"/><TextBlock Text="Existing features are located by layer name and FacilityID. New features keep their recorded FacilityID. GlobalIDs are not used for target matching." Foreground="{DynamicResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,5,20,0"/></StackPanel><Button Grid.Column="1" Content="Open package" Margin="8,0" Click="OpenPackage_Click"/><Button x:Name="PlayButton" Grid.Column="2" Content="Play to active map" Style="{StaticResource PrimaryButton}" Click="Play_Click" IsEnabled="False"/></Grid></Border><DataGrid x:Name="PlaybackGrid" Grid.Row="1" Margin="0,12,0,0" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="#" Binding="{Binding Sequence}" Width="50"/><DataGridTextColumn Header="Operation" Binding="{Binding Type}" Width="120"/><DataGridTextColumn Header="Layer" Binding="{Binding LayerName}" Width="*"/><DataGridTextColumn Header="Facility ID" Binding="{Binding FacilityId}" Width="150"/><DataGridTextColumn Header="Global ID (audit only)" Binding="{Binding SourceGlobalId}" Width="210"/></DataGrid.Columns></DataGrid></Grid></TabItem>
    </TabControl>
    <Border Grid.Row="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="18,10"><TextBlock x:Name="StatusText" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" TextWrapping="Wrap"/></Border>
  </Grid></Border>
</Window>
